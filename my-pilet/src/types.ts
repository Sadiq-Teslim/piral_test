/**
 * Defines the structure for a Post object.
 * This aligns with the data from https://jsonplaceholder.typicode.com/posts
 */
export interface Post {
  userId: number;
  id: number;
  title: string;
  body: string;
}

